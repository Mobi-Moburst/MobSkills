# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# misc
.DS_Store
*.pem

# env files (can opt-in for committing if needed)
.env*

# Local agent settings — this repo is public.
.claude/

# Local-only cruft. No build lives here any more (the UI moved to MobPulse), but
# these paths still exist in working copies from the portal era and must never be
# committed to a public repo. .vercel in particular would carry org/project ids.
/node_modules
/.next/
.vercel
*.tsbuildinfo
next-env.d.ts
.data/

# Plans live in MobPulse (docs/agent-skills/) — they describe MobPulse
# internals and must never be committed to this public repo.
plans/
